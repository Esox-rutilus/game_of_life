const lol = 'lol'

/**
 * Returns false when passed a string
 * 
 * @param {string} myString A string to pass
 * @return {boolean}
 */
function tester(myString) {
    if (myString) {
        return false;
    }
    else {
        return true;
    }
} 